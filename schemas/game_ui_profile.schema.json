{
  "$defs": {
    "ReferenceVariant": {
      "additionalProperties": false,
      "description": "One hash-pinned full-frame reference with anchor patches and named controls.\n\n``matching=\"white_glyph\"`` (profile version 2) compares binarized white glyph\nshapes instead of raw pixels. It exists for advertisement-network chrome such\nas skip/close/mute icons drawn on translucent disks over arbitrary creatives,\nwhere raw RGB similarity is dominated by the creative behind the glyph. Such\nvariants may carry a single anchor because the glyph shape itself is the\ncreative-independent evidence; they are restricted to the advertisement state,\nand a click still requires the control glyph to match at its own threshold.",
      "properties": {
        "label": {
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "state": {
          "enum": [
            "playing",
            "paused",
            "revive_offer",
            "bonus_offer",
            "result",
            "tune",
            "advertisement",
            "unknown"
          ],
          "title": "State",
          "type": "string"
        },
        "file": {
          "title": "File",
          "type": "string"
        },
        "sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "title": "Sha256",
          "type": "string"
        },
        "anchors": {
          "items": {
            "maxItems": 4,
            "minItems": 4,
            "prefixItems": [
              {
                "type": "integer"
              },
              {
                "type": "integer"
              },
              {
                "type": "integer"
              },
              {
                "type": "integer"
              }
            ],
            "type": "array"
          },
          "minItems": 1,
          "title": "Anchors",
          "type": "array"
        },
        "controls": {
          "additionalProperties": {
            "maxItems": 4,
            "minItems": 4,
            "prefixItems": [
              {
                "type": "integer"
              },
              {
                "type": "integer"
              },
              {
                "type": "integer"
              },
              {
                "type": "integer"
              }
            ],
            "type": "array"
          },
          "title": "Controls",
          "type": "object"
        },
        "matching": {
          "default": "rgb",
          "enum": [
            "rgb",
            "outlined_white",
            "white_glyph"
          ],
          "title": "Matching",
          "type": "string"
        },
        "notes": {
          "default": "",
          "title": "Notes",
          "type": "string"
        }
      },
      "required": [
        "label",
        "state",
        "file",
        "sha256",
        "anchors"
      ],
      "title": "ReferenceVariant",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "version": {
      "default": 1,
      "enum": [
        1,
        2
      ],
      "title": "Version",
      "type": "integer"
    },
    "profile_id": {
      "title": "Profile Id",
      "type": "string"
    },
    "expected_size": {
      "default": [
        1034,
        581
      ],
      "maxItems": 2,
      "minItems": 2,
      "prefixItems": [
        {
          "type": "integer"
        },
        {
          "type": "integer"
        }
      ],
      "title": "Expected Size",
      "type": "array"
    },
    "threshold": {
      "default": 0.97,
      "maximum": 1,
      "minimum": 0.97,
      "title": "Threshold",
      "type": "number"
    },
    "margin_pixels": {
      "default": 4,
      "maximum": 8,
      "minimum": 0,
      "title": "Margin Pixels",
      "type": "integer"
    },
    "ambiguity_margin": {
      "default": 0.01,
      "maximum": 0.1,
      "minimum": 0,
      "title": "Ambiguity Margin",
      "type": "number"
    },
    "glyph_threshold": {
      "default": 0.9,
      "maximum": 1,
      "minimum": 0.85,
      "title": "Glyph Threshold",
      "type": "number"
    },
    "glyph_control_threshold": {
      "default": 0.95,
      "maximum": 1,
      "minimum": 0.9,
      "title": "Glyph Control Threshold",
      "type": "number"
    },
    "evidence_scope": {
      "default": "local construction references; live reset unvalidated",
      "title": "Evidence Scope",
      "type": "string"
    },
    "variants": {
      "items": {
        "$ref": "#/$defs/ReferenceVariant"
      },
      "minItems": 1,
      "title": "Variants",
      "type": "array"
    }
  },
  "required": [
    "profile_id",
    "variants"
  ],
  "title": "GameUIProfile",
  "type": "object"
}
