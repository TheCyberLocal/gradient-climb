# Canonical resumption plan after Research Cycle 1

Status: **PAUSED by the owner.** This document is the future work queue, not authorization or a scheduler. The [pause directive](../methodology/cycle-1-pause-directive.md) supersedes the original open-ended execution mandate. No new experiment, native interaction, draft restoration or training should start merely because a historical protocol says pending.

The containing release is bound by the immutable annotated tag `cycle-1-paused`; its annotation/release receipt records the final development/main SHAs and CI checks. [resume-state.md](resume-state.md) owns operational release status. Do not confuse the stabilization starting SHA `f8183772d78641dc692a1008152af9ba085207bf` with the final release revision. The [completion audit](../methodology/completion-audit.md) distinguishes release readiness from incomplete scientific goals.

**Exact first resumption task:** after an explicit user request to resume research, verify the paused release and local evidence, then preregister Cycle 2 objective/behavioral metrics and a bounded native reset/scoring reliability test before new training. The operational frontier is reliable scored native episodes. A reward campaign must not precede that reliability gate.

## Recover the evidence without repeating experiments

Check the tag/receipt, clean repository state, pinned environment and [canonical integrity inventory](../../research/experiments/cycle-1-integrity.json). At the pause, 100 canonical runs were finalized and hash/seal verified; no unfinished run or uncatalogued run directory was found. Hash verification establishes file integrity, not validity of scientific conclusions or existence of an external backup. Data/checkpoints/game images remain in ignored `artifacts/`; Git alone is insufficient to reproduce the analyses.

Verify required profiles, glyph banks, source frame hashes and checkpoint parents before using them. The [auxiliary local evidence inventory](../../research/experiments/cycle-1-local-evidence.json) records files outside the canonical run store, including UI references. Important local drafts and their tests are recorded in [deferred-screen-dynamics.md](../research/deferred-screen-dynamics.md). Do not restore them automatically. Read the finalized report/notebooks, [stabilization validation](stabilization-validation.md) and [hypothesis disposition](../../research/hypotheses/cycle-1-status.md) before selecting work so completed experiments are not duplicated.

## Prioritized remaining work

All future execution below is **DEFERRED**. The current state describes what already exists; completion criteria describe what would justify closing the original gap. Any numerical acceptance threshold newly proposed here must be frozen in the resumed protocol before data collection.

| Priority / work | Current status | Prerequisites and implementation locations | Intended evidence and completion criterion |
| --- | --- | --- | --- |
| 0. Objective and endpoint registration | Design only | [reward-research.md](../research/reward-research.md), `experiments/definitions/`, run configuration/metadata. Preserve Cycle 1 objectives unchanged. | Version distance, score, time, termination/recovery, censoring and selection rules; distinguish optional new metrics from currently measurable fields. Record a bounded reliability protocol before any training. |
| 1. Native reset/scoring/capture reliability | Partial; last pilot failed on unknown ad | `control/game_adapter.py`, `control/session.py`, `capture/screen.py`, scoring readers, saved pilot `51d2527e`. Begin offline with the recorded failure, then reacquire the actual window only when native work resumes. | Fresh identity/focus/geometry; four independent pedal transitions; held-out UI/score checks; a preregistered consecutive episode series spanning natural result and full-horizon pause, with every score/parking outcome accounted for. Suggested minimum ten attempts and no unintended action, plus explicit success/unknown rates. Unknown ads remain no-click; use legitimate controls only. |
| 2. Independent real observation labels | Sparse | `perception/annotations.py`, `measurements.py`, `hud.py`, `scoring.py`, `screen_features.py`. Existing profiles/glyphs are construction sources. | Separate episodes/sessions labeled before tuning; coordinate/angle/terrain error and exact numeric accuracy with coverage and failures. Validate fuel/score/recovery fields before objectives depend on them. No world-unit claim from uncalibrated pixels. |
| 3. Qualified real baselines and learning pilot | Baselines n=2; CEM no update | `scripts/run_screen_episodes.py`, `scripts/train_screen_cem.py`, `algorithms/screen_search.py`; frozen 34-parameter actor and known score rules. Reliability gate first. | Random, always-gas and a declared stabilizing baseline with matched settings and exclusions; at least ten completed evaluation episodes per selected policy where feasible. A short learned pilot must complete an eligible generation/update and independent checkpoint evaluation. Training candidate scores alone do not qualify a policy. |
| 4. Governed actual-game benchmark | Missing | Prior class, initial weights, clean source, versioned profiles/metrics, checkpoint schedule and declared clock. | Full 3600 s measured training with 5/10/20/30/45/60-minute checkpoint evaluations, actual overshoot, costs/reset waits and independent real evaluations. Provisional distance gate: median ≥500 m, ≥3× matched random median and ≥8/10 reaching 250 m. Preserve this historical distance criterion while evaluating future objectives separately; no human-expert claim without a reference distribution. |
| 5. Dynamics strategy decision | Missing real calibration/comparison | Existing `calibration/effective.py`; optional ignored screen-dynamics draft only after explicit adoption review. Trustworthy action coverage, independent episode splits and labels required. | Compare persistence/history/action-conditioned prediction with held-out errors in observed units and rank/omission counts. Calibrated physical model needs real training/held-out trajectory evidence; a direct-learning alternative needs measured actual quality/time advantages. Neither a fit nor plausible randomization alone closes this gate. |
| 6. Teacher/student and sim-to-real | Implemented student, never trained | `algorithms/screen_distillation.py`, `scripts/distill_screen_student.py`, [student protocol](../research/screen-body-distillation.md), verified teacher checkpoint. | Measure student agreement and rollout quality, then identical deployable checkpoint on real/sim observations with gap/coverage. Account for teacher and distillation prior compute. Analytic silhouette projection is not validated pixel alignment. |
| 7. Real generalization and adaptation | Missing; synthetic partial | Qualified source policy; explicitly available vehicle/map configurations, no purchases; zero-shot evaluation before adaptation. | New real map, vehicle and both; 0/5/10/30/60-minute adaptation curves with parent lineage, actual exposure and source retention. Independent training seeds/episodes, context versus no-context comparison if adopted. Do not relabel synthetic heavy/rough as a commercial vehicle/map. |
| 8. Multi-objective/recovery learning | New design, no measured fields or training | Priorities 0–3; validated score/recovery labels and [reward proposal](../research/reward-research.md). | Four required frozen equal-budget arms: distance-only, distance+raw score, distance+score+time, and recovery-conditioned; optional lexicographic arm. Full outcome vectors, eight hacking diagnostics, failure/coverage audits and Pareto tradeoffs. Demonstrate strategic benefit beyond raw score, preserve distance baseline and report negative results. |
| 9. Remaining methodology/presentation | Partial infrastructure | Runtime/evaluation/report/notebook scripts, dashboard and artifacts. | Matched architecture baselines and selected longer ablations if justified, controlled live headed training/overhead, early/later/adaptation and paired real/sim videos, independent reproduction/uncertainty, final source-linked report. No empty empirical panel filled with synthetic substitutes. |
| 10. Curriculum | Unimplemented/untested; conditional on need | Diagnose repeatable failure regions from qualified trajectories; `simulation/hill.py`, trainer configuration and evaluation protocols. | Freeze a difficulty schedule and compare against unchanged sampling at equal total time/experience and identical held-out tasks. Adopt only a replicated quality/time gain without hiding curriculum construction cost. |
| 11. Privileged critic | Not experimentally tested | A validated deployable actor observation path; `algorithms/ppo.py`, `policies.py` or a new explicitly separated critic interface. | Hold actor inputs/architecture fixed, vary critic privileged information only, report equal-budget learning and real deployment quality. Critic state must never leak into actor decisions. Main Cycle 1 oracle PPO is not this experiment. |
| 12. Hyperparameter optimization | Manual bounded runtime screen only | Qualified evaluation/clock and search-space budget preregistration; `experiments/definitions/`, existing training/evaluation scripts. | Specify random/adaptive search allocation and stopping rules, log every trial/prior cost, select on validation only and evaluate frozen winners on untouched tasks. Compare search benefit with spending the same time on training; no expensive framework required without evidence. |
| 13. Long-horizon and algorithm comparisons | Short PPO/CEM screen plus one source extension | Reliable baseline/learned endpoint; same observation/action/reward/horizon and declared prior classes; `algorithms/`, governed runner and benchmark schemas. | Compare promising original/established PPO, recurrent or evolutionary alternatives under matched actual time with independent training seeds; retain transitions/resource costs and failure distributions. Measure selected longer training beyond one hour separately, including plateau/regression and transfer. Do not promote a one-minute configuration result to general superiority. |

The historical car was Hill Climber on Countryside, upgrades engine 13/13, suspension 14/14, tires 16/16, drivetrain 10/10. User bindings were Right Arrow gas and Left Arrow brake. Cached window coordinates, handles and device initialization are not a current discovery result. Explicit capture backend selection exists; a DXcam initialization failure is a failure, not permission to silently change a registered backend.

## Disposition of prior plans

| Plan / dispatched work | Cycle 1 disposition | What remains |
| --- | --- | --- |
| [one-hour-surrogate.json](../../experiments/definitions/one-hour-surrogate.json) | COMPLETED | Primary and independent-seed reproduction plus saved checkpoint evaluations; no rerun to refresh a report. |
| [post-hour-battery.json](../../experiments/definitions/post-hour-battery.json) | COMPLETED as dispatched | 23 jobs, including 15 learning runs and derived evaluations; actual learning 5523.3294751 s against requested 5520 s. Its original `REGISTERED_PENDING_DISPATCH` text is historical registration, not current queue state. [Final plan disposition](../../research/experiments/cycle-1-plan-status.json) contains job/run mappings. |
| [ablations.json](../../experiments/definitions/ablations.json) | NEVER STARTED; DEFERRED | Original 300 s × seeds 10/11/12 screen. Completed 60 s × seeds 101/102/103 battery is a separate experiment. |
| [adaptation-pending.json](../../experiments/definitions/adaptation-pending.json) | NEVER STARTED as registered; DEFERRED | Three-seed, three-condition 5/10/30/60-minute plan. A single ten-minute heavy/rough child does not complete it. |
| [real-screen-cem.json](../../experiments/definitions/real-screen-cem.json) | PARTIAL dispatch / FAILED pilots; DEFERRED | Three capture initialization failures and one attempted candidate ending in ad/parking failure. Zero eligible episodes, no update, no governed hour. Start with reliability, not a resumed fictitious learned checkpoint. |
| Restricted student protocol | NEVER STARTED; DEFERRED | Implementation/tested interface only; no distillation run or transfer metric. |
| Observable-dynamics manifests/drafts | DRY PLAN ONLY; DEFERRED | Preserve ignored files; no fit or active policy context. |
| New objective/recovery proposal | DESIGN ONLY; DEFERRED | No code change or retrospective rescoring of canonical results. |

No plan row authorizes automatic execution. After the stabilized cycle is merged, development stops until the owner resumes it.
