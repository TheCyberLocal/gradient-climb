# Cycle 3 resumption roadmap

Status: **research paused by the owner on 2026-09-12**. The owner redirected work
to stabilization, presentation, pushing `dev`, merging and pushing `main`, and
documenting the remaining work. This is a future-work queue, not a scheduler or
permission to execute an experiment. No fitting, training, capture, reader
evaluation or new research starts while paused, including work allowed by an
earlier registration. Resumption requires a new explicit owner instruction.

The requested integration into `main` is a software milestone. It does not declare
the scientific program complete or qualify a learned controller. The
[stabilized handoff](cycle-3-stabilized-state.md) and
[closing report](../../research/reports/cycle-3-stabilization-report.md) own the
closing evidence and validation receipts. Preserve Cycle 1 and Cycle 2 records, tags, source revisions,
metrics and audit cutoffs; add new evidence instead of rewriting their conclusions.

**Episodes measure experience. Compute measures cost. Wall-clock time measures
rapidity. Real-game capability determines whether the learning mattered.** The
[learning-efficiency methodology](../methodology/learning-efficiency.md) governs
future comparisons. Raw simulator throughput, synthetic test success and
construction selfchecks cannot substitute for independently evaluated real
competence. No qualifying learned real-game threshold has yet been demonstrated.

## 1. Recover the exact starting point and preserve the evidence

On authorized resumption, verify the closing source revision, branch/tag ancestry,
CI, environment and local artifacts. Read the
[requirements register](../methodology/cycle-3-evidence-register.md),
[campaign ledger](../../experiments/definitions/cycle-3-campaign.json) and the
latest current-state addenda before choosing work. Resolve the final reader
declaration and annotation-publication receipts from that record; do not assume
an attempted publication completed or create a replacement silently.

The 136-run and 149-run integrity inventories remain historical cutoffs. The
[151-run closing inventory](../../research/experiments/cycle-3-stabilization-integrity-001.json)
adds the final declaration and annotation publication. Verify later runs separately
or create a new explicitly dated inventory. Preserve failed,
interrupted and unscored records, their original counters and their source hashes.
Git contains source and reviewable metadata, not all private pixels, footage,
checkpoints or canonical payloads.

A private backup destination remains unresolved. Archive only to an owner-approved
destination and demonstrate a hash-verified restore before claiming a backup.
Existing archive/restore software or a successful local integrity check does not
establish that such a backup exists. This work can proceed independently of reader
or learner selection after resumption.

## 2. Finish the bounded reader construction before considering qualification

The [registered construction supplement](../../experiments/definitions/cycle-3-reader-construction-001.json)
reserves **one unused builder invocation**: one reviewed full numeric region from
native session 2, attempt 8, `terminal-024.png`; append its three glyphs to the
58-glyph bank and perform one construction selfcheck. The builder and candidate
freeze were not invoked before the stop. Registration remains evidence of the
planned dose; it does not override the pause.

Before that invocation, verify the completed annotation envelope, declaration,
explicit known-exposure audit, exact original frame, full reader/anchor/UI graph,
source commit and available disk. Preserve the thresholds, ambiguity margin,
alignment, numeric ROI, anchor and UI. The selected source is decoded three times
by this builder; inherited glyph and UI loads are additional work. Record the
actual costs and all failures. A fault or nonmatching selfcheck ends the registered
attempt without alternate images, tuning or an automatic retry. Any successor
requires a prospective decision and registration.

Freeze only the exact completed, sealed candidate with the expected 58+3 glyphs
and successful declared selfcheck. Then construct its complete successor ancestry
from actual output hashes. The source is already exposed construction material;
its selfcheck cannot establish reader accuracy. Historical native attempt 8
remains `success_unscored` with null qualified distance, irrespective of the visible
269 m construction label or the new reader's eventual output.

## 3. Register enough fresh reader evidence and finish native reliability

The [reader qualification protocol](../../experiments/definitions/cycle-3-reader-validation-3.0.json)
requires fresh acquisition after candidate freeze, whole-session purpose and
selection receipts, and immutable labels sealed before prediction exposure. Its
minimum gates include 20 accepted positive endpoints, at least 90% positive
availability, accepted positives across three heldout sessions, all ten digits
among correctly accepted positives, ten non-result negatives and five unreadable
result negatives, with negative evidence across three sessions and zero observed
wrong accepts. Report all labeled, available and accepted denominators. These are
finite evidence gates, not proof of zero population error.

The [native collection hooks](reader-native-collection-3.md) are implemented and
synthetically tested; their acquisition amendment remains draft. Register the
concrete acquisition dose, source, scenario, timing, negative selectors and
prediction-hidden review procedure before any native collection. Keep the first
retained `terminal_frame` callback for each endpoint, all preregistered negatives,
all bound source runs and attempts without retained endpoints. Earlier gameplay
result pixels and later animation frames cannot be substituted to improve a
denominator. Known exposure cannot be erased with a new audit or session name.

Native reliability still failed: the completed session had 11 accepted scores in
12 attempts and a longest scored streak of eight, below the required ten. Two of
the three registered reliability sessions are used; **only one remains**. That
single remaining session cannot satisfy a requirement for three fresh heldout
reader sessions. A prospective, compatible collection plan or budget amendment is
required; do not split one continuous session or silently extend the old budget.
Also retain the separate reliability gates of ten consecutive scored successes,
at least ten scored attempts out of twelve, and zero unintended actions or manual
interventions. Qualified readers do not themselves prove operational reliability.

After those gates, a separately registered representative soak must account for
30 attempts, waits, reset costs and every safety failure. Full-horizon stopping,
censoring and safe release need evidence; the earlier natural-ending attempts did
not exercise all of these paths. Unattended native training remains blocked until
its required instrument and soak evidence exists.

## 4. Turn existing demonstrations and the owner archive into usable evidence

The owner's direction is to use the existing YouTube archive instead of requesting
more human recordings. Preserve the synchronized local captures already obtained,
but do not request new play sessions under this roadmap. Archive footage remains
construction material with its own acquisition and review costs.

The bounded local review produced 54 four-image windows from 60 unique images.
They remain construction-only and training-ineligible because named profile/build
identity and complete experience are unresolved. Further review must establish
profile evidence, frame and action timing, segment boundaries, missingness and
whole-session partitions before training eligibility changes. The 745 m pause is
a selected progress lower bound, not a natural terminal result or complete human
benchmark.

The archive has no synchronized action labels. Visible pedal hypotheses require
an explicit extraction method, timing uncertainty and independent validation;
actionless footage cannot be presented as behavioral-cloning supervision. Mask
nuisance overlays under a frozen observation contract. Separate construction
footage from untouched benchmark evidence, and verify vehicle, map, upgrades,
build, horizon and assistance before comparing human and policy outcomes. If the
archive cannot establish a matched benchmark, retain that gap. The owner's
reported 2,000 m capability is not an independently verified distribution.

## 5. Measure fidelity before selecting a simulator or observation pipeline

The articulated-engine pilot establishes engineering feasibility. The four-pose
screen is a small construction diagnostic: simple rendered wheels can be easier
to detect than real ones, and measurable terrain conventions remain limited. Neither
result qualifies physical dynamics, visual accuracy or sim-to-real transfer.

Register construction and heldout geometry/dynamics splits, coordinate and terrain
boundary conventions, validity masks, tolerances and cost budgets. Resolve measured
body/wheel/terrain failures before increasing visual complexity. Compare history,
action-conditioned prediction and proposed physics/rendering fidelity on untouched
trajectories, then test whether the added fidelity improves independently evaluated
real control enough to justify its full cost. Retain unsuccessful fits and respect
the campaign's bounded fit rounds. Throughput alone cannot select the winner.

## 6. Select learners and objectives with matched evidence

Choose at most the registered number of candidate methods and freeze concrete
architectures, observation history, replay/curriculum or privileged-critic use,
search budgets, seeds and checkpoints before screening. A deployable actor must
not receive hidden state reserved for training diagnostics or a privileged critic.
Compare against declared simple baselines, then replicate promising methods;
negative screens require a specific diagnosis rather than an indefinitely repeated
recipe. Native training additionally requires the instrument gates above.

Keep cold start, demonstration-assisted learning, simulator pretraining,
generalist adaptation and fine-tuning distinct. Separately declare engineered or
task-trained system priors: readers, renderers, calibration, teachers, archive
data and human review. Report own, inherited and evaluation costs, deduplicating
shared ancestors without summing parallel elapsed times. CPU core-seconds,
accelerator utilization, transitions, simulator episodes/seconds, physics steps,
renders, decisions, updates and real interaction retain their units and coverage.
Unknown historical cost or experience stays unknown.

After distance and pace baselines are dependable, validate score and recovery
measurements before running the required distance, distance+score,
distance+score+time and recovery-conditioned arms. Freeze matched budgets,
noninferiority margins and hacking diagnostics. Require conserved unique score
credit and full survival/distance/pace/score outcome vectors; a nominal reward
increase does not establish useful skill. Additional lexicographic comparisons
must preserve true ordering and the same accounting.

## 7. Execute the governed one-hour qualification only when dispatchable

The [learning-efficiency framework](../../experiments/definitions/cycle-3-learning-efficiency.json)
is still not dispatchable. Bind the selected method, exact source, scenario,
policy/system priors, untouched evaluation manifest, instrument evidence, entry
clock and resource/fidelity budgets first. The reference scenario requires freshly
verified Hill Climber/Countryside settings, including actual upgrades and build.

The registered framework uses three 3,600-second training seeds and checkpoints at
5/10/20/30/45/60 minutes. Evaluate each unique frozen checkpoint on 20 scheduled
real episode executions under the fixed 900-second horizon and endpoint rules.
Report threshold medians at 500, 1,000 and 2,000 real metres; the practical reference
target is median at least 2,000 m, alongside quartiles, worst outcome, pace and
natural failures. No outcome-selected replacement, late checkpoint backdating or
post-deadline learner update is allowed.

Training production time and independent verification latency are different.
The framework's maximum evaluation exposure is 360 episodes, up to 90 gameplay
hours and 108 batch wall hours, separate from three training hours; it does not
schedule those bounds automatically. Report first observed threshold attainment
and its sampling bracket. Censor at the last eligible evaluated checkpoint; no
eligible evaluation means not evaluable. Preserve missing batches, unchanged
checkpoint hashes and failed seeds. Never claim a finite speedup against an
unresolved censored comparator or equate surrogate units with real metres.

## 8. Demonstrate transfer, adaptation and retention, then close the program

After reference competence, register at least two additional vehicle profiles and
one additional map using legitimately available content. Evaluate zero-shot
behavior before adaptation, then 5/10/30/60-minute target curves and source-task
retention. Keep inherited training visible beside adaptation cost, and test map,
vehicle and combined changes under profile-specific frozen targets. Do not rename
synthetic presets as verified commercial profiles or purchase content implicitly.

Close remaining evidence-register items with matched real/sim and early/later/
adaptation demonstrations, independent reproduction, uncertainty and source-linked
reports. Reports, dashboard and notebooks must use the same canonical efficiency
analysis and preserve missing/censored outcomes. A final scientific completion
claim requires this evidence; a clean repository, green CI or successful merge
alone establishes software validation and source availability.
