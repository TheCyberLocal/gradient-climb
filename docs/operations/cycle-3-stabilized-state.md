# Cycle 3 stabilized handoff — 2026-09-12

The owner requested: “Stabilize current progress presentably and push to dev then
merge and push to main and document future work and goals and conclude with a
report of the details.” Research dispatch stops at this boundary. This later
instruction authorizes a software checkpoint on `main` while the scientific
program remains incomplete. Earlier milestone statements requiring scientific
qualification before merging do not govern this owner-authorized integration.
Version **0.1.0a2** and both historical cycle tags remain unchanged; this is not a
new release or a claim that Cycle 3 achieved its research targets.

Read the [closing report](../../research/reports/cycle-3-stabilization-report.md)
for results and validation, the [future-work queue](cycle-3-future-work.md) for
resumption gates, and the [chronological work record](cycle-3-current-state.md)
for earlier cutoffs. The [evidence register](../methodology/cycle-3-evidence-register.md)
and [campaign ledger](../../experiments/definitions/cycle-3-campaign.json)
retain the original registrations and failed attempts.

## Exact research stop

The latest collection/publication source is clean commit
`1c80c3265a2a296db384d9b3ec44de87fa915553`. The completed declaration
`02886e4c-b804-4c0d-a619-9eda13b01ee9` and annotation publication
`ac65bbd6-1c8e-4b7a-a57f-23fc1efa895d` add one known-exposure construction label
for the existing right-side result-distance field, **269**. The original native
attempt's rejected numeric reading remains **null** and its reliability gate
remains failed. See the [stop receipt](../../research/diagnostics/reader3-construction-stop-001.md).

No reader builder invocation, candidate freeze, fresh native capture, training or
independent qualification followed this publication. The registered one-image
builder dose is unused; registration is not an instruction to resume it. The
native acquisition amendment remains **draft and non-dispatchable**. Native
receipt collection hooks are implemented and tested, but have not collected a
live validation session. Two of three reliability sessions were used, including
the foreground failure. One remains; it cannot satisfy the reader's three-session
heldout requirement on its own. The opt-in reader route enforces the cumulative
cap; the unchanged legacy route still requires campaign dispatch governance.

## Evidence and local dependencies

The [new integrity cutoff](../../research/experiments/cycle-3-stabilization-integrity-001.json)
verified **151/151 canonical runs**, **2,107,956,898 bytes**, with no unfinished
runs or uncatalogued directories, at **2026-09-12 17:45:20 UTC**. Earlier
136/149-run cutoffs retain their original meaning. Failed runs are part of this
verified inventory. A valid seal establishes retained byte identity, not scientific
validity, label truth, blinding or a recoverable backup.

Raw run directories, videos, images, controls, models and telemetry remain local
under ignored `artifacts/`. The current construction plans/logs are under
`artifacts/reader3-construction-001/`; canonical payloads are in `artifacts/runs/`.
The existing native source checkout at `artifacts/cycle3-native-source/` has shared
artifact junctions; inventory the canonical run store directly rather than
recursively traversing that checkout. Git contains code, protocols and derived
reports, not the private evidence payloads. Preserve the pinned local reader/UI
files, optional engine dependency and acquisition tool versions referenced by
their source records. A private backup destination and verified restore remain
pending; see [evidence preservation](cycle-3-evidence-preservation.md).

No learned real-game competence threshold or qualifying learning-efficiency
envelope exists at this stop. The governing principle remains:

> **Episodes measure experience. Compute measures cost. Wall-clock time measures rapidity. Real-game capability determines whether the learning mattered.**
