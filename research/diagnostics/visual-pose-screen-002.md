# Four-pose construction screen 002

The registered successor completed once on 2026-09-12 as canonical run `19955a1f-58aa-4e41-9371-bc2f7ee43da1`, using tracked source commit `982682fc2b386f299722f0408e9dec4e5f7561f4`. All four source images were decoded, all four poses were rendered once, and all four pairs and exact actor PNGs were retained. The seal verifies 44 files; the directory including its seal occupies 2,081,886 bytes, below 32 MiB. The original registration and frozen labels were unchanged. [Complete numeric interpretation](visual-pose-screen-002.json), [canonical paired output](../../artifacts/runs/19955a1f-58aa-4e41-9371-bc2f7ee43da1/visual-pose-screen.json), [dispatch log](../../artifacts/cycle3-visual-pose-screen-002.log).

This passes the engineering retention and accounting checks. It does not establish independent perception accuracy, game fidelity or competence. Selection followed earlier detector inspection, annotation uncertainty is an uncalibrated manual estimate, and incomplete profile identity keeps the source training-ineligible. There were zero physics steps, simulator seconds, episodes, policy decisions, optimizer updates and new real-game interaction.

| Original frame | Source wheel/terrain valid | Render wheel/terrain valid | Body-center residual to labels, source/render | Terrain median absolute residual, source/render |
| --- | --- | --- | --- | --- |
| 39 | No / No | Yes / No | 4.54 / 3.04 px | Unavailable / Unavailable |
| 350 | Yes / Yes | Yes / Yes | 3.41 / 1.78 px | 2.09 / 3.77 px |
| 358 | No / Yes | Yes / Yes | 3.60 / 2.35 px | 1.65 / 3.91 px |
| 378 | Yes / No | Yes / No | 4.65 / 1.32 px | Unavailable / Unavailable |

All eight body detections were valid hypotheses. Their residuals compare a coarse polygon area centroid with a visible color-component pixel centroid; these are different constructions, and neither is center of mass. Declared uncertainty is ±4 px for body geometry, ±3 px for wheel circles and ±6 px for terrain. Those estimates are not accuracy thresholds. The JSON retains every measured body, wheel and terrain result, every available point discrepancy, invalid reasons and source/derived hashes.

The procedural wheels are too simple to reproduce source ambiguity. Source 39 rejects a pair among three circular candidates; source 358 rejects a pair among two candidates. Both rendered pairs pass. Every rendered wheel center lies 0.7071 px from its manual label, but this close agreement says little about real-image robustness. For 350/378, rendered radii are 10.8 px while source radii span 13.8–15.2 px. This is consistent with selecting the inner hub edge: the renderer draws hubs at 0.68 of tire radius. Candidate-level causation was not tested, and no output-selected adjustment was made.

The terrain convention also needs attention. On 350 and 358, rendered signed mean y residuals are +3.7908 px and +3.8757 px. The code centers a 6 px green stroke on labels explicitly describing the lower turf/material edge, making the observed downward offset plausible. Source comparison uses 48 and 25 visible samples, versus 48 and 24 for the render; no missing sample or labeled gap is filled. Source and render 39/378 both reject terrain for lacking a horizontally extensive turf component, consistent with the frozen ROI/width requirement. This does not mean no terrain exists. Near-vertical 378 still has only modulo-π orientation in both actor paths; landmark metadata adds no actor cue.

Command wall reported by the shell tool was 4.9749 s, including Python startup and finalization. Publisher entry to the pre-report timestamp was 3.7691 s; recorder wall was 3.7303 s with a different start boundary. The nested replay took 0.3135 s, comprising 0.01440 s rendering and 0.09681 s shared perception plus hashing/callback work. Source hash/decode took 0.07198 s, source perception 0.16257 s, and PNG publication 0.12579 s. These components overlap the larger scopes and must not be added to them.

The recorder measured 4.515625 process CPU core-seconds and a 98,430,976-byte sampled peak RSS. Five process samples cover about 98.50% of the 3.4265 s resource window. Two device-wide GPU queries yield 1.4856 utilization-equivalent seconds at 98.54% coverage; this is neither renderer attribution nor energy. Background tests were permitted during dispatch, and exact overlap was not independently logged. Other applications also contribute to GPU and host-memory values. This is a construction cost receipt, not a throughput comparison or evidence of rapid learning.

The recorded worktree is dirty: the captured tracked source diff is empty, but unrelated reader/downloader files are untracked, and documentation was being edited. Their names and hashes are retained in the source snapshot; their contents are not embedded. The dispatch used the committed tracked implementation, without claiming the whole checkout was clean. Public interpretation omits process identities, GPU identity and host paths.

Inherited costs remain separate. The source human capture lasted 91.1891 s, with 106.1353 s recorder wall and 63.953125 CPU core-seconds. Its later window-unavailable failure is preserved. Earlier failed window assembly, completed assembly and geometry measurement retain their distinct costs in the linked prior report. Screen 001 failed before recorder creation, with 0.87185 s tool-reported wall and unmeasured CPU/GPU/memory; its log and failure report remain intact. Annotation labor, human practice and detector/renderer development time remain unknown, not zero. [Prior construction costs](imitation-construction-002.json), [screen 001 failure](visual-pose-screen-001-preflight-failure.json).

The next prospective step is a bounded, separately registered renderer-convention and wheel-appearance diagnosis using the unchanged labels and fixed synthetic fixtures. Broader disjoint reviewed evidence is needed before a fidelity or control claim. No additional replay, rendering, fitting or collection was performed for this interpretation.
